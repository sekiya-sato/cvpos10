<helpers:BaseWindow x:Class="CvPos10.Views._06Uriage.PosProductSearchView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:helpers="clr-namespace:CvPos10.Helpers" xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:vm="clr-namespace:CvPos10.ViewModels._06Uriage"
	Title="商品検索"
	Width="900" Height="700" MinWidth="700" MinHeight="500"
	Background="{DynamicResource AppCommonBackgroundBrush}"
	FocusManager.FocusedElement="{Binding ElementName=SearchTextBox}"
	Foreground="{DynamicResource MaterialDesignBody}"
	ResizeMode="NoResize" TextOptions.TextFormattingMode="Display"
	mc:Ignorable="d">
	<helpers:BaseWindow.DataContext>
		<vm:PosProductSearchViewModel />
	</helpers:BaseWindow.DataContext>

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>

		<!--  ヘッダー  -->
		<materialDesign:ColorZone Grid.Row="0"
			Padding="12,8"
			materialDesign:ElevationAssist.Elevation="Dp4" Mode="PrimaryMid">
			<StackPanel Orientation="Horizontal">
				<materialDesign:PackIcon Width="28" Height="28"
					VerticalAlignment="Center"
					Kind="Magnify" />
				<TextBlock Margin="10,0,0,0" VerticalAlignment="Center"
					FontSize="20" FontWeight="SemiBold" Text="商品検索" />
			</StackPanel>
		</materialDesign:ColorZone>

		<!--  検索欄  -->
		<Grid Grid.Row="1"
			Margin="16,12,16,0">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*" />
				<ColumnDefinition Width="Auto" />
			</Grid.ColumnDefinitions>
			<TextBox x:Name="SearchTextBox"
				Grid.Column="0"
				materialDesign:HintAssist.Hint="商品コード・名称・JANコード" materialDesign:TextFieldAssist.HasClearButton="True"
				Style="{StaticResource FormTextBox}"
				Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}">
				<TextBox.InputBindings>
					<KeyBinding Command="{Binding SearchCommand}"
						Key="Enter" />
				</TextBox.InputBindings>
			</TextBox>
			<Button Grid.Column="1"
				Margin="12,0,0,0"
				Command="{Binding SearchCommand}"
				Content="検索"
				Style="{StaticResource PosPrimaryButton}" />
		</Grid>

		<!--  結果リスト  -->
		<Grid Grid.Row="2"
			Margin="16,8,16,0">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*" />
				<ColumnDefinition Width="*" />
			</Grid.ColumnDefinitions>

			<!--  商品一覧  -->
			<materialDesign:Card Grid.Column="0"
				Margin="0,0,8,0" Padding="0"
				materialDesign:ElevationAssist.Elevation="Dp1" UniformCornerRadius="8">
				<Grid>
					<Grid.RowDefinitions>
						<RowDefinition Height="Auto" />
						<RowDefinition Height="*" />
					</Grid.RowDefinitions>
					<Border Grid.Row="0"
						Padding="12,8"
						Background="{DynamicResource MaterialDesignToolBarBackground}"
						CornerRadius="8,8,0,0">
						<TextBlock FontWeight="SemiBold" Text="商品一覧" />
					</Border>
					<ListView Grid.Row="1"
						ItemsSource="{Binding Products}"
						SelectedItem="{Binding SelectedProductMaster}">
						<ListView.View>
							<GridView>
								<GridViewColumn Width="120"
									DisplayMemberBinding="{Binding Code}"
									Header="コード" />
								<GridViewColumn Width="*"
									DisplayMemberBinding="{Binding Name}"
									Header="名称" />
								<GridViewColumn Width="80"
									DisplayMemberBinding="{Binding TankaJodai, StringFormat={}{0:N0}}"
									Header="上代" />
							</GridView>
						</ListView.View>
					</ListView>
				</Grid>
			</materialDesign:Card>

			<!--  SKU 一覧  -->
			<materialDesign:Card Grid.Column="1"
				Margin="8,0,0,0" Padding="0"
				materialDesign:ElevationAssist.Elevation="Dp1" UniformCornerRadius="8">
				<Grid>
					<Grid.RowDefinitions>
						<RowDefinition Height="Auto" />
						<RowDefinition Height="*" />
					</Grid.RowDefinitions>
					<Border Grid.Row="0"
						Padding="12,8"
						Background="{DynamicResource MaterialDesignToolBarBackground}"
						CornerRadius="8,8,0,0">
						<TextBlock FontWeight="SemiBold" Text="SKU 一覧" />
					</Border>
					<ListView Grid.Row="1"
						ItemsSource="{Binding Skus}"
						SelectedItem="{Binding SelectedSku}">
						<ListView.View>
							<GridView>
								<GridViewColumn Width="140"
									DisplayMemberBinding="{Binding Mei_Col}"
									Header="カラー" />
								<GridViewColumn Width="100"
									DisplayMemberBinding="{Binding Mei_Siz}"
									Header="サイズ" />
								<GridViewColumn Width="120"
									DisplayMemberBinding="{Binding Jan1}"
									Header="JAN" />
							</GridView>
						</ListView.View>
					</ListView>
				</Grid>
			</materialDesign:Card>
		</Grid>

		<!--  フッター  -->
		<StackPanel Grid.Row="3"
			Margin="16" Orientation="Horizontal">
			<ProgressBar Margin="0,0,16,0"
				IsIndeterminate="True"
				Style="{StaticResource MaterialDesignLinearProgressBar}"
				Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}" />
			<TextBlock VerticalAlignment="Center"
				Style="{StaticResource StatusTextBlockStyle}"
				Text="{Binding StatusMessage}" />
			<Button Margin="16,0,0,0"
				Command="{Binding SelectSkuCommand}"
				Content="選択"
				IsDefault="True"
				Style="{StaticResource PosPrimaryButton}" />
		</StackPanel>
	</Grid>
</helpers:BaseWindow>
