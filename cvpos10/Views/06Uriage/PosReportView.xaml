<helpers:BaseWindow x:Class="CvPos10.Views._06Uriage.PosReportView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:helpers="clr-namespace:CvPos10.Helpers" xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:vm="clr-namespace:CvPos10.ViewModels._06Uriage"
	Title="各種レポート"
	Width="1100" Height="750" MinWidth="900" MinHeight="600"
	Background="{DynamicResource AppCommonBackgroundBrush}"
	Foreground="{DynamicResource MaterialDesignBody}"
	TextOptions.TextFormattingMode="Display"
	mc:Ignorable="d">
	<helpers:BaseWindow.DataContext>
		<vm:PosReportViewModel />
	</helpers:BaseWindow.DataContext>

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>

		<!--  ===== ヘッダー =====  -->
		<materialDesign:ColorZone Grid.Row="0"
			Padding="12,8"
			materialDesign:ElevationAssist.Elevation="Dp4" Mode="PrimaryMid">
			<DockPanel>
				<StackPanel DockPanel.Dock="Left" Orientation="Horizontal">
					<materialDesign:PackIcon Width="28" Height="28"
						VerticalAlignment="Center"
						Kind="ChartBar" />
					<TextBlock Margin="10,0,0,0" VerticalAlignment="Center"
						FontSize="20" FontWeight="SemiBold" Text="各種レポート" />
				</StackPanel>
				<StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
					<Button Command="{Binding ExitCommand}"
						Style="{StaticResource ToolCommandButton}"
						ToolTip="閉じる (Esc)">
						<materialDesign:PackIcon Kind="Close" />
					</Button>
				</StackPanel>
			</DockPanel>
		</materialDesign:ColorZone>

		<!--  ===== レポート種別 + 日付範囲 + 集計ボタン =====  -->
		<StackPanel Grid.Row="1"
			Margin="16,12,16,0" Orientation="Horizontal">
			<ComboBox Width="180"
				DisplayMemberPath="DisplayName"
				ItemsSource="{Binding ReportTypeItems}"
				SelectedValue="{Binding SelectedReportType}"
				SelectedValuePath="Value" />
			<DatePicker Margin="12,0,0,0"
				SelectedDate="{Binding FromDate}" />
			<TextBlock Margin="8,0"
				VerticalAlignment="Center" Text="～" />
			<DatePicker SelectedDate="{Binding ToDate}" />
			<Button Margin="12,0,0,0"
				Command="{Binding LoadCommand}"
				Content="集計"
				Style="{StaticResource PosPrimaryButton}" />
		</StackPanel>

		<!--  ===== レポートグリッド =====  -->
		<materialDesign:Card Grid.Row="2"
			Margin="16" Padding="0"
			materialDesign:ElevationAssist.Elevation="Dp2" UniformCornerRadius="8">
			<Grid>
				<Grid.RowDefinitions>
					<RowDefinition Height="Auto" />
					<RowDefinition Height="*" />
				</Grid.RowDefinitions>
				<Border Grid.Row="0"
					Padding="12,8"
					Background="{DynamicResource MaterialDesignToolBarBackground}"
					CornerRadius="8,8,0,0">
					<TextBlock FontWeight="SemiBold" Text="集計結果" />
				</Border>
				<DataGrid Grid.Row="1"
					materialDesign:DataGridAssist.CellPadding="12,6" materialDesign:DataGridAssist.ColumnHeaderPadding="12,8"
					AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False"
					ColumnHeaderStyle="{StaticResource MenteDataGridColumnHeader}"
					EnableRowVirtualization="True" GridLinesVisibility="Horizontal"
					HorizontalGridLinesBrush="{DynamicResource MaterialDesignDivider}"
					IsReadOnly="True"
					ItemsSource="{Binding ReportRows}"
					Style="{StaticResource MaterialDesignDataGrid}"
					VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling">
					<DataGrid.Columns>
						<DataGridTextColumn Width="120"
							Binding="{Binding Key1}"
							Header="コード" />
						<DataGridTextColumn Width="*"
							Binding="{Binding Label}"
							Header="名称" />
						<DataGridTextColumn Width="90"
							Binding="{Binding Count, StringFormat={}{0:N0}}"
							ElementStyle="{StaticResource DataGridRightTextBlock}"
							Header="件数" />
						<DataGridTextColumn Width="90"
							Binding="{Binding Quantity, StringFormat={}{0:N0}}"
							ElementStyle="{StaticResource DataGridRightTextBlock}"
							Header="点数" />
						<DataGridTextColumn Width="140"
							Binding="{Binding Amount, StringFormat={}{0:N0}}"
							ElementStyle="{StaticResource DataGridRightTextBlock}"
							Header="金額" />
					</DataGrid.Columns>
				</DataGrid>
			</Grid>
		</materialDesign:Card>

		<!--  ===== フッター =====  -->
		<Grid Grid.Row="3"
			Margin="16,0,16,16">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*" />
				<ColumnDefinition Width="Auto" />
			</Grid.ColumnDefinitions>
			<StackPanel Grid.Column="0" VerticalAlignment="Center">
				<ProgressBar Margin="0,0,0,4"
					IsIndeterminate="True"
					Style="{StaticResource MaterialDesignLinearProgressBar}"
					Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}" />
				<TextBlock Style="{StaticResource StatusTextBlockStyle}"
					Text="{Binding StatusMessage}" />
			</StackPanel>
		</Grid>
	</Grid>
</helpers:BaseWindow>
